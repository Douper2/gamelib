import setup
from setup import find_packages

setup(
    name='gamelib',
    version='0.1',
    description='Library to make games in Python.',
    author='Douper',
    packages=find_packages(),
)