import pygame

def mouse_pressed(bool):
    pygame.mouse.get_pressed(bool)

def mouse_get_pos(x, y):
    pygame.mouse.get_pos(x, y)

def mouse_set_pos(x, y):
    pygame.mouse.set_pos(x, y)

def mouse_set_visible(bool):
    pygame.mouse.set_visible(bool)

