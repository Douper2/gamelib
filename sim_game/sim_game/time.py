import pygame

def time_wait(int):
    pygame.time.wait(int)

def delay_game(int):
    pygame.time.delay(int)

if isinstance(time_wait(), str):
    raise SyntaxError("Cannot use string in time_wait() function")

if isinstance(time_wait(), str):
    raise SyntaxError("Cannot use string in delay_time() function")